# Calibration Fixtures

- `calibration-policy-valid.yaml`:
  - Valid calibration routing policy used by `calibration-routing-policy.test.js`.
  - Covers task routing (`coding`, `general`) with primary/fallback chains.
